import styled from "styled-components";

export default function TopicMarkdown() {
  return <Container>Topic Markdown</Container>;
}

const Container = styled.div`
  grid-area: 3 / 2 / 8 / 5;
  display: flex;
  justify-content: center;
  align-items: center;
`;
